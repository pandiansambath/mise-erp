# ─────────────────────────────────────────────────────────────
# SECRETS — never commit. (Credentials live only on the dev machine.)
# ─────────────────────────────────────────────────────────────
github_token.txt
*accessKeys*.csv
pandiansambath_accessKeys.csv
.env
.env.*
!.env.example
*.pem
*.key
secrets/
credentials*

# ─────────────────────────────────────────────────────────────
# CLIENT BUSINESS DATA — real PII / financials. Never commit.
# (Kept locally as reference only.)
# ─────────────────────────────────────────────────────────────
*.xlsx
*.xls
*.pdf
client_data/
raw_data/

# ─────────────────────────────────────────────────────────────
# Python
# ─────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
coverage.xml
*.db
*.sqlite3
uploads/
backend/uploads/

# ─────────────────────────────────────────────────────────────
# Node / Next.js
# ─────────────────────────────────────────────────────────────
node_modules/
.next/
out/
.turbo/
npm-debug.log*
yarn-error.log*
*.tsbuildinfo

# ─────────────────────────────────────────────────────────────
# Terraform
# ─────────────────────────────────────────────────────────────
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# ─────────────────────────────────────────────────────────────
# Editor / OS / tooling-local
# ─────────────────────────────────────────────────────────────
.claude/settings.local.json
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# stray archives
*.zip

# Resend API key (local only)
docs/resend_key.txt
*resend_key*

# Stock-media + AI API keys (local only)
docs/pixabay_api_and_doc.txt
docs/pexels_api_key_and_documentation_link.txt
docs/falai_for_image.txt
docs/gemini_api_key.txt
*pixabay*api*
*pexels*api*
*falai*
*gemini*api*key*
docs/journey-promy.txt
previous_fable_chat.txt

# Local AI asset originals (web-optimized copies live in frontend/public/experience)
generated_assets/
ui_ideas/

# Database backups — CLIENT DATA, never commit (kept locally + in S3 db-backups/)
backups/
*.sql
*.sql.gz
ssm_backup.json

# Real client data drops (used for AI autofill testing) — never commit
real_hotel_datas/
