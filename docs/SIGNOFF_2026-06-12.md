# Sign-off — 2026-06-12 session (for the next AI / Opus)

## Shipped & LIVE ✅ (commit `b2764c7`, deploy run 27416790960 success, verified on http://18.133.95.137)

1. **Dark premium dashboard** (per `docs/DASHBOARD_THEME_BRIEF.md`) — the logged-in app now
   matches the landing's dark green-black aurora look. Semantic tokens (`shell/paper/paper-2/
   paper-3/line/line-2/fg/fg-soft/fg-faint`) live in a **NON-inline `@theme`** in
   `frontend/app/globals.css` (inline would bake hexes — see brief §0). The **6-accent theme
   switcher still works** (brand-* untouched). `color-scheme: dark` for native controls.
   Shell aurora is in `AppShell.tsx` (`ShellAurora`, dialled to opacity-40).
2. **App-wide motion**: route transitions via `app/(app)/template.tsx` (`.mise-page`),
   staggered KPI grids (`.mise-stagger`), pop-in dropdowns/modals (`.mise-pop`), drawer
   slide, button micro-press — all in globals.css, all respect `prefers-reduced-motion`.
3. **Chef-friendly pickers (#3 Phase 2)**: new `frontend/components/ItemPicker.tsx`
   (multi + single-select). Category **tabs with emoji**, big tappable cards with **stock
   pills 🟢🟡🔴**, search, "Your list" tray with **kg + g** entry. Wired into **Purchasing**
   (indent form), **Recipes** (ingredients), **Price Comparison** (item choose), **Vendors**
   (price form + type pills). Inventory category ComboBox now seeds 16 standard categories
   (add-new still works; categories drive the picker tabs — keep names consistent).
4. Fixes: stale e2e expectations updated (landing hero "Everything in its place", login
   "Welcome back", Documents `exact: true`); purchasing confirm text says **chosen supplier**;
   recipe margin figure tones green/amber/red.

**Verification:** ruff ✓ tsc ✓ eslint ✓ next build ✓ Playwright **54/54** ✓ (a 3.9h "failure"
run earlier was the laptop sleeping — re-run was 1.6m). Live CSS bundle confirmed to contain
the new tokens. ⚠️ PS 5.1 gotcha: `Get-Content` mangles UTF-8 → use explicit UTF8 IO for bulk
edits (one corruption was caught & reverted this session).

## NOT done — next up (in order)

1. **#3 Phase 3 — per-indent supplier override** (researched, no code yet):
   - `IndentItem.vendor_id` nullable FK + migration (head = `b2c3d4e5f6a7_document_requests`).
   - `IndentItemIn/Out` + `create_indent` accept it; `generate_pos` uses override-else-chosen
     (override must be an active vendor pricing that item, else skip+flag).
   - `POSummary.vendor_name` + show in PO table; supplier shown per indent line.
   - Frontend: supplier pills under the Purchasing tray (needs a bulk
     `GET /purchasing/item-suppliers` endpoint); keep overrides in a separate
     `Record<item_id, vendor_id>` state (don't widen `PickedLine`).
2. **Costing follow-up**: `recipes/service._best_price` still falls back to cheapest —
   switch to **chosen-only** (preferred → avg-cost → missing-flag), update
   `test_recipes.py:74` (asserts cheapest), inventory `best_vendor` → chosen-only,
   and the Price Comparison footnote text.
3. **BATCH 5** (memory checklist): typeahead everywhere, dup-prevention (vendors/channels),
   export back-fill, auth extras, light-mode toggle (tokens make this easy now).

## Ideas backlog (money-first brainstorm, user pre-approved adding)

- **Break-even widget** on dashboard: daily sales target from fixed costs vs today's net — low effort, high wow.
- **Stock value KPI** (Σ qty × avg cost = cash tied up in stock).
- **"Order all low items"** one-click indent from the low-stock list.
- **Waste/consumption logging UI** — backend movements exist, no UI → stock goes stale (borderline bug).
- **Vendor price-rise alerts** → show affected recipe margins.
- **VAT summary export** (UK quarterly returns).
- Per-dish sales capture (unlocks menu engineering) — bigger.

Runbook (build→test→deploy→verify) is in `docs/DASHBOARD_THEME_BRIEF.md` §4–5; deploys are
manual via GitHub Actions "Deploy (eu-west-2)". Never touch the landing. No AI attribution.
