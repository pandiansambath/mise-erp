# Runtime dependencies — pinned for reproducibility (Python 3.12)
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
python-jose[cryptography]==3.3.0
# Was already here as a python-jose extra; now a DIRECT dependency, because
# the attendance PIN is encrypted with Fernet so the owner can be shown it
# again. Pinned to the version already running in production (probed, not
# guessed) — relying on another package's extra for something we import
# ourselves is how a dependency disappears in an unrelated upgrade.
cryptography==50.0.1
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
httpx==0.28.1
openpyxl==3.1.5
fpdf2==2.8.2
greenlet==3.1.1
# 1.40 is the FLOOR, not a preference: `freetier:GetAccountPlanState` — the
# call that returns the remaining AWS credit, the single most important number
# on the money dashboard — does not exist in botocore before 1.40.0. I checked
# the shipped service model rather than guessing: 1.35.80 and 1.38.0 expose
# only `GetFreeTierUsage`; 1.40.0 is where `GetAccountPlanState` and
# `ListAccountActivities` appear.
#
# This mattered because the calling code degrades gracefully on failure, so the
# old pin would not have crashed — it would have quietly served the
# hand-entered credit figure forever while the page claimed to read it live.
boto3==1.40.76
tzdata==2024.2
sentry-sdk[fastapi]==2.19.2
segno==1.6.1
